using System; // Importa o namespace System, que contém classes básicas, incluindo Console para entrada/saída

// Declaração da classe principal do programa
class Program
{
    // Método estático chamado HelloWorld
    // Este método não recebe parâmetros e não retorna nada (void)
    static void HelloWorld()
    {
        // Escreve a mensagem "Hello World!" no console
        Console.WriteLine("Hello World!");
    }
}
